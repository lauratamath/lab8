<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/start2">

    <fragment
        android:id="@+id/start2"
        android:name="com.example.lab8.views.Start"
        android:label="fragment_start"
        tools:layout="@layout/fragment_start" >
        <action
            android:id="@+id/action_start2_to_repository"
            app:destination="@id/repository" />
    </fragment>
    <fragment
        android:id="@+id/repository"
        android:name="com.example.lab8.views.Repository"
        android:label="fragment_repository"
        tools:layout="@layout/fragment_repository" />
</navigation>